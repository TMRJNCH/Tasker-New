# 📘 Руководство пользователя Tasker Enterprise

Добро пожаловать в **Tasker Enterprise** — профессиональную систему для управления задачами и проектами. Этот документ поможет вам начать работу с системой.

---

## 1. Введение
**Tasker** предназначен для менеджеров и разработчиков, которым нужно быстро создавать задачи, выставлять приоритеты и получать уведомления о критических событиях в Telegram.

**Ключевые возможности:**
*   🚀 Мгновенное создание задач.
*   ⚡️ Поддержка критических приоритетов.
*   📱 Интеграция с мессенджерами.

---

## 2. Начало работы

### Установка
Для работы требуется Python 3.10+.
1. Скачайте последнюю версию проекта.
2. Установите зависимости командой:
   ```bash
   pip install -r requirements.txt
   ```

### Запуск сервера
Чтобы начать пользоваться системой, запустите API-сервер:
```bash
uvicorn src.main:app --reload
```
После запуска откройте в браузере: `http://127.0.0.1:8000/docs`.

---

## 3. Основной функционал (User Stories)

### Сценарий 1: Создание новой задачи
**Как пользователь**, я хочу создать задачу, чтобы не забыть о ней.

1. Откройте интерфейс API (`/docs`).
2. Найдите раздел **POST /api/v1/tasks**.
3. Нажмите **Try it out**.
4. Заполните поля:
   ```json
   {
     "title": "Подготовить отчет",
     "priority": "medium"
   }
   ```
5. Нажмите **Execute**. Система вернет созданную задачу с уникальным ID.

### Сценарий 2: Критическая задача
**Как руководитель**, я хочу, чтобы о срочных задачах приходило уведомление.

1. При создании задачи выберите приоритет `critical`.
2. Система автоматически отправит сообщение в рабочий чат Telegram.

---

## 4. Справочник API

| Метод | URL | Описание |
| :--- | :--- | :--- |
| **GET** | `/api/v1/tasks` | Получить список всех задач |
| **POST** | `/api/v1/tasks` | Создать новую задачу |
| **GET** | `/health` | Проверка статуса сервера |

---

## 5. Часто задаваемые вопросы (FAQ)

**В: Куда сохраняются задачи?**
О: В текущей версии используется In-Memory база данных. При перезапуске сервера список очищается. В версии Pro будет подключен PostgreSQL.

**В: Как настроить Telegram?**
О: Впишите ваш токен бота в файл конфигурации `src/services/telegram.py`.
```

### Шаг 2. Зафиксируйте в Git
Теперь нужно отправить эту документацию в репозиторий.

```bash
git add MANUAL.md
git commit -m "Docs: Added comprehensive User Manual"
git push
```
